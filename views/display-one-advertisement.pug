extends layout

block content
	h1 THE ad

	div(data-objData=data)
	
	p= "From: " + data.from + ", To: " + data.to + ", Availbale seats:" + data.available_seats + ", Driver: " + data.driver + ", Departure: " + data.departure + ", Arriving: " + data.arrival
	if data.confirmed_riders
		p="Rider: "+ data.confirmed_riders
	br
	if data.interested_riders
		p="Candidate Rider: "+ data.interested_riders
	br

	
	form(method= 'POST', action='/rides/hop-on-ride/'+data.id)

		input.btn.btn.primary(type='submit', value='Hope on the ride bruh')

	form(method= 'POST', action='/rides/hop-off-ride/'+data.id)

		input.btn.btn.primrary(type='submit', value='Hope off the ride bruh')
